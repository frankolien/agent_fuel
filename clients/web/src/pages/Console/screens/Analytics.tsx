import { Screen, ScreenPlaceholder } from "./Screen";

export function Analytics() {
  return (
    <Screen eyebrow="Insights" title="Analytics" subtitle="Spend, reputation, and policy trends over time.">
      <ScreenPlaceholder note="Analytics charts will follow Agents and Vaults UIs." />
    </Screen>
  );
}
