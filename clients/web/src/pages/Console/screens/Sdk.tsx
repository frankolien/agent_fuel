import { Screen, ScreenPlaceholder } from "./Screen";

export function Sdk() {
  return (
    <Screen eyebrow="Reference" title="SDK" subtitle="API reference for @agent-fuel/sdk.">
      <ScreenPlaceholder note="SDK reference will mirror what ships with @agent-fuel/sdk in Phase 4.S." />
    </Screen>
  );
}
