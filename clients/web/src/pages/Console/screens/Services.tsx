import { Screen, ScreenPlaceholder } from "./Screen";

export function Services() {
  return (
    <Screen eyebrow="Catalog" title="Services" subtitle="Whitelisted x402 providers your agents can pay.">
      <ScreenPlaceholder note="Services catalog will follow Vaults UI." />
    </Screen>
  );
}
